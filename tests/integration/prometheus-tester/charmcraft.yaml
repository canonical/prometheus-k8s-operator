type: charm
bases:
  - build-on:
    - name: "ubuntu"
      channel: "20.04"
    run-on:
    - name: "ubuntu"
      channel: "20.04"
parts:
  charm:
    build-packages:
    - git
    charm-binary-python-packages:
      - jsonschema
      - cryptography
      - pyyaml
      - requests
      - ops
      - cosl
      - pydantic>=2
